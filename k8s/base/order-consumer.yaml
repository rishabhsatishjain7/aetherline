apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-consumer
  namespace: aetherline
spec:
  replicas: 2
  selector:
    matchLabels: {app: order-consumer}
  template:
    metadata:
      labels: {app: order-consumer}
    spec:
      containers:
        - name: order-consumer
          image: aetherline/order-service:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "app.consumer"]
          env:
            - name: POSTGRES_USER
              valueFrom: {secretKeyRef: {name: aetherline-db-credentials, key: POSTGRES_USER}}
            - name: POSTGRES_PASSWORD
              valueFrom: {secretKeyRef: {name: aetherline-db-credentials, key: POSTGRES_PASSWORD}}
            - name: DATABASE_URL
              value: "postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@order-db:5432/orders"
          envFrom:
            - configMapRef: {name: aetherline-config}
          resources:
            requests: {cpu: "100m", memory: "128Mi"}
            limits: {cpu: "500m", memory: "256Mi"}
